import { SchemaColumnTypes } from '@/types/resourceManager/schemaColumnTypes';
import functions from './gofakeit-functions.json';

/** All available `types` in Schema columns */
export const schemaColumnTypes: SchemaColumnTypes = functions;
